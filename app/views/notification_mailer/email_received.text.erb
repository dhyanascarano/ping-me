Hi,

You've received an email to Ping Me from <%= @data.fetch(:from) %>.

Here's what they wrote:

<%= @data.fetch(:body) %>

====================
Don't want to receive emails to this address again? Click the link below:
<%= temporary_contact_detail_delete_url(@data.fetch(:details).id) %>
